1) Implementar em Python o modelo cliente/servidor com sockets.

a) O servidor deve atender um único pedido do cliente: Retornar o horário do momento solicitado. O cliente deve solicitar o horário ao servidor e encerrar. O servidor deve ser multithread. Cole o link dos arquivos no repositório (git).

b) O servidor deve escutar em uma porta específica (ex: 8000). Quando recebe uma string, ele a reverte e a envia de volta. O cliente que se conecta ao servidor, envia uma string ("Olá Mundo Distribuído") e espera a resposta. Cole o link dos arquivos no repositório (git).
